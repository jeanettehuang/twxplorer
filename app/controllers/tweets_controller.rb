class TweetsController < ApplicationController
  def index
    # if params[:search] != nil 
    #   system "rake loadtweetdb[" + params[:search] + "]"
    # end
    # # Tweet.reindex
    # 
    # @tweets = Tweet.find_by_sql(["SELECT * FROM tweets WHERE query='boston'"])
    
    # @search = Tweet.search do
    #   params[:search]
    # end
  end
end
